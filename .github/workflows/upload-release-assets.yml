name: Upload Release Assets

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build all platform binaries
        run: make release

      - name: Upload assets to v3.0.0 release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v3.0.0
          files: dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
name: Upload Release Assets

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build all platform binaries
        run: make release

      - name: Upload assets to v3.0.0 release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v3.0.0
          files: dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

